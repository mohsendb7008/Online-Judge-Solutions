#include <bits/stdc++.h>
using namespace std;

// Bijele

int k, q, r, b, n, p;

int main(){
    cin >> k >> q >> r >> b >> n >> p;
    cout << 1 - k << " " << 1 - q << " " << 2 - r << " " << 2 - b << " " << 2 - n << " " << 8 - p << endl;
}